import type { SxProps } from "@mui/material";

const gridItem: SxProps = { xs: 1, md: 5 };

const gridContainer: SxProps = {
  justifyContent: "center",
};
export default { gridItem, gridContainer };
