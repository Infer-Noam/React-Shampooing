import type { SxProps } from "@mui/material";

const typography: SxProps = {
  display: "flex",
  alignItems: "center",
};

const paper: SxProps = {
  p: 1,
  borderRadius: "16px",
  display: "flex",
  flexDirection: "row",
};

export default { typography, paper };
